#include <stdio.h>

int main(void)
{
    int a;
    printf("정수 입력 : ");
    scanf("%d",&a);
    if(a < 0)
    {
        printf("%d\n",a);
        printf("minus\n");
    }
    if (a > 0)
    {
        printf("%d\n",a);
    }
    return 0;
}
