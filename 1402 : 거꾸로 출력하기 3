#include <stdio.h>

int main(void)
{
    int a[1001] = { 0 };
    int n;
    int i;
    scanf("%d",&n);
    for(i = 0; i < n; i++)
    {
        scanf("%d",&a[i]);
    }
    for(int i = n - 1; i >= 0; i--)
    {
        printf("%d ",a[i]);
    }
    return 0;
}
