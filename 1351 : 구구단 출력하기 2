#include <stdio.h>

int main(void)
{
    int a,b;
    scanf("%d %d",&a,&b);
    for(; a <= b; a++)
    {
        for(int i = 1; i <= 9; i++)
        {
            printf("%d*%d=%d\n",a,i,a * i);
        }
    }
    return 0;
}
