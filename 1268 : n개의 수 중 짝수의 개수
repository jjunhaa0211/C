#include <stdio.h>

int main(void)
{
    int a,b,t = 0;
    scanf("%d",&a);
    for(int i = 1; i <= a; i++)
    {
        scanf("%d",&b);
        if(b % 2 == 0)
        {
            t = t + 1;
        }
    }
    printf("%d",t);
    return 0;
}
