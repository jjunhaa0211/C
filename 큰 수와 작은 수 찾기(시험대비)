#include <stdio.h>
int main(void)
{
    int a,b;
    printf("정수 두 개를 입력하세요. : ");
    scanf("%d %d",&a,&b);
    if(a < b)
    {
        printf("입력 받은 수 중 큰 수는 %d이고, 작은 수는 %d입니다.",b,a);
    }
    if(a > b)
    {
        printf("입력 받은 수 중 큰 수는 %d이고, 작은 수는 %d입니다.",a,b);
    }
    return 0;
}
