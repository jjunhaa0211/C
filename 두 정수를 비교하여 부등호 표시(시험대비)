#include <stdio.h>
int main(void)
{
    int a,b;
    printf("정수 두 개를 입력하세요. : ");
    scanf("%d %d",&a,&b);
    if(a < b)
    {
        printf("%d < %d",a,b);
    }
    if(a > b)
    {
        printf("%d > %d",a,b);
    }
    if(a == b)
    {
        printf("%d == %d",a,b);
    }
    return 0;
}
