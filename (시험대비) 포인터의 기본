#include <stdio.h>

int main(void)
{
    int num = 10;
    int* pnum = &num;
    
    printf("변수 num의 값 : %d\n",num);
    printf("&num(주소값) : %p\n",&num);
    printf("포인터 변수 pnum의 값 : %p\n",pnum);
    return 0;
}
