#include <stdio.h>

int n2;
void odd(int num1)
{
    if(num1 == 0) return;
    odd(num1 - 1);
    if(num1 % 2 == 1) printf("%d\t",num1);
    
}

int main(void)
{
    int n1;
    printf("두 수를 입력하세요 : ");
    scanf("%d %d",&n1,&n2);
    odd(n2);
    return 0;
}
