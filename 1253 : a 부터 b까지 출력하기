#include <stdio.h>

int main(void)
{
    int a,b;
    scanf("%d %d",&a,&b);
    if(a > b)
    {
        for(int i = b; i <= a; i++)
        {
            printf("%d ",i);
        }
    }
    else if(a < b)
    {
        for(int j = a; j <= b; j++)
        {
            printf("%d ",j);
        }
    }
    else
    {
        printf("%d",a);
    }
    return 0;
}
