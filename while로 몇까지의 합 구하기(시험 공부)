#include <stdio.h>

int main(void)
{
    int a;
    int i = 1;
    int sum = 0;
    printf("정수 입력 : ");
    scanf("%d",&a);
    while (i <= a) {
        sum = sum + i;
        i++;
    }
    printf("1부터 %d까지의 합 : %d",a,sum);
    return 0;
}
