#include <stdio.h>

int n;

void f(int a)
{
    if(a < 0)
    {
        printf("negative");
    }
    else if(a == 0)
    {
        printf("zero");
    }
    else
    {
        printf("positive");
    }
}
int main()
{
  scanf("%d", &n);
  f(n);
  return 0;
}
