#include <stdio.h>

int main(void)
{
    int n,m;
    int i;
    int max = 0;
    scanf("%d",&n);
    for(i = 0; i < n; i++)
    {
       scanf("%d",&m);
       if(m > max)
       {
           max = m;
       }
    }
    printf("%d",max);
    return 0;
}
