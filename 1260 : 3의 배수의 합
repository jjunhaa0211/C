#include <stdio.h>

int main(void)
{
    int n1,n2;
    int sum = 0;
    scanf("%d %d",&n1,&n2);
    int a;
    for(a = n1; a <= n2; a++)
    {
        if(a % 3 == 0)
        {
            sum = sum + a;
        }
    }
    printf("%d",sum);
    return 0;
    
}
