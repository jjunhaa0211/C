#include <stdio.h>

int squared(int a, int b)
{
    int sum = a;
    for(int i = 1; i < b; i++)
    {
        sum = sum * a;
    }
    return sum;
}

int main(void)
{
    int n1,n2;
    scanf("%d %d",&n1,&n2);
    printf("%d",squared(n1, n2));
    return 0;
}
