#include <stdio.h>

int main(void)
{
    int a;
    printf("자연수를 입력하세요. : ");
    scanf("%d",&a);
    if(a % 2 == 0)
    {
        printf("even");
    }
    else if (a % 2 == 1)
    {
        printf("odd");
    }
    return 0;
}
