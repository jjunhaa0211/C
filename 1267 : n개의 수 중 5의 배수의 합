#include <stdio.h>

int main(void)
{
    int a = 0;
    int n;
    int sum = 0;
    scanf("%d",&n);
    for(int i = 0; i < n; i++)
    {
        scanf("%d",&a);
        if(a % 5 == 0)
        {
            sum = sum + a;
        }
    }
    printf("%d",sum);
    return 0;
}
