#include <stdio.h>

int main(void)
{
    int a,b;
    printf("정수 두 개를 입력하세요.(0 제외) : ");
    scanf("%d %d",&a,&b);
    printf("%d + %d = %d\n",a,b,a+b);
    printf("%d - %d = %d\n",a,b,a-b);
    printf("%d * %d = %d\n",a,b,a*b);
    printf("%d / %d = %d\n",a,b,a/b);
    return 0;
}
